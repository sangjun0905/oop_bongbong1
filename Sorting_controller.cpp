// Sorting_controller.cpp
#include "Sorting_controller.hpp"
#include <iostream>

// 생성자
Sorting_controller::Sorting_controller(Views* v, Service* s) 
    : views(v), service(s) {}

void Sorting_controller::run(){
    int option =0;
    while (true){
        switch (option){
            case 1:
            case 2:
            case 3:
            case 4:
            default:
        }
    }
}